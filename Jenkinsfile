pipeline {
  agent any
  stages {
    stage('Initialize') {
      steps {
        echo 'hallo'
      }
    }
    stage('') {
      steps {
        sh 'tar -cvzf  ~/git_last.tar.gz ~/.git'
      }
    }
  }
}